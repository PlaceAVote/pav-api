machine:
  pre:
    - wget http://dynamodb-local.s3-website-us-west-2.amazonaws.com/dynamodb_local_latest
    - tar -xvzf dynamodb_local_latest
    - java -Djava.library.path=./DynamoDBLocal_lib -jar DynamoDBLocal.jar --sharedDb:
        background: true
    - curl -Ls https://install.convox.com/linux.zip > /tmp/convox.zip
    - sudo unzip /tmp/convox.zip -d /usr/local/bin
    - convox login convox-879923626.us-west-2.elb.amazonaws.com --password tknkFFiDruFRJOqRAmesmJNhChlPdu
  java:
    version: oraclejdk8
  services:
    - redis
deployment:
  development:
    branch: master
    commands:
      - convox build


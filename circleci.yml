dependencies:
  pre:
    - sudo apt-get update
    - sudo wget http://dynamodb-local.s3-website-us-west-2.amazonaws.com/dynamodb_local_latest
    - sudo tar -xvzf dynamodb_local_latest
    - java -Djava.library.path=. -jar DynamoDBLocal.jar:
        background: true
test:
  commands:
    - lein midje
machine:
  java:
    - oraclejdk8
  services:
    - redis
